# Application Settings
NODE_ENV=development
PORT=3000

# Database Configuration
# Match these with your docker-compose.yml settings for local dev
DB_HOST=db
DB_PORT=5432
DB_USER=switchboard
DB_PASSWORD=super_secure_password_here
DB_NAME=switchboard

# Add your secure secrets below in future steps (e.g., JWT_SECRET, ENCRYPTION_KEY)